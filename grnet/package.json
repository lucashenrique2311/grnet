{
  "name": "grnet",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start": "expo start",
    "android": "expo start --android",
    "ios": "expo start --ios",
    "web": "expo start --web",
    "build:preview": "eas build --platform android --profile preview --clear-cache",
    "build:production": "eas build --platform android --profile production --clear-cache",
    "build:ios": "eas build --platform ios --profile production --clear-cache"
  },
  "engines": {
    "node": ">=20.19.4"
  },
  "dependencies": {
    "@expo/vector-icons": "^15.0.0",
    "babel-preset-expo": "^54.0.1",
    "expo": "54.0.33",
    "expo-linear-gradient": "~15.0.7",
    "expo-status-bar": "~3.0.8",
    "react": "19.2.4",
    "react-native": "0.83.1",
    "react-native-webview": "13.15.0"
  },
  "devDependencies": {
    "@babel/core": "^7.20.0"
  },
  "private": true
}
